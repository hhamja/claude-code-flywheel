---
description: "flywheel 루프 상태 요약 (루프·백로그·BLOCKED·최근 기록)"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/loop-status.sh:*)"]
---

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/loop-status.sh"
```

위 출력을 사용자에게 그대로 전달하라. BLOCKED 가 있으면 그 해결 방법
(BLOCKED.md 의 질문에 답 → 파일 삭제 → 재개)만 한 줄 덧붙여라. 그 외 해석은 붙이지 마라.
